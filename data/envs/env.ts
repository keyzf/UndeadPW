export default class ENV {
  public static APPLY_URL = String(process.env.APPLY_URL);
  public static APPLY_DOMAIN = String(process.env.APPLY_DOMAIN);
  public static SKYNET_URL = String(process.env.SKYNET_URL);
  public static SKYNET_API = String(process.env.SKYNET_API);
}
